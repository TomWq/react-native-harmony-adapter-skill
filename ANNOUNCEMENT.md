# Share Blurb

## Chinese

我们整理了一套可复用的 Codex Skill：`react-native-harmony-adapter`。

这套 skill 基于真实的 React Native + Expo Router 项目鸿蒙适配过程沉淀而来，覆盖依赖盘点、Harmony 宿主工程接线、compat wrapper、Expo Router 稳定化、图标字体处理、自定义 TurboModule 接入等关键步骤，适合用于现有 RN 项目的 HarmonyOS / OpenHarmony 迁移工作。

## English

We packaged a reusable Codex skill: `react-native-harmony-adapter`.

This skill was distilled from a real Harmony migration of a React Native + Expo Router application. It covers dependency audits, Harmony host-project wiring, compat wrappers, Expo Router stabilization, icon/font fixes, and custom TurboModule integration for HarmonyOS / OpenHarmony adaptation work.
